<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- %logger{N} controls class name length: 0 = class name only, higher N = more of the package shown -->
            <pattern>%d{HH:mm:ss} [%-5level] %logger{0} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- level="DEBUG" shows debug, info, warn, error -->
    <!-- level="INFO" hides debug, shows info, warn, error -->
    <!-- level="WARN" hides debug and info, shows warn, error -->
    <!-- level="OFF" hides everything -->
    <logger name="com.mathcat" level="DEBUG"/>

    <root level="WARN">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
